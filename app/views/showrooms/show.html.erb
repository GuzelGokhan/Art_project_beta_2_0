<% content_for :meta_title, "#{@showroom.exhibition.galery.name} | #{@showroom.name}" %>
<div class="navbar nav navbar-expand-sm navbar-light navbar-lewagon">
  <div class="nav-item">
    <ul class="navbar-nav ml-auto">
      <li><%= link_to "Home", root_path, class: "nav-link", style: "text-decoration: none; color: gray;"   %></li>
      <li> <%= link_to "Back", galery_path(@showroom.exhibition.galery) , :class => "nav-link", style: "text-decoration: none; color: gray;"%></li>
    </ul>
  </div>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
      <li class="nav-item dropdown">
        <span class="nav-link dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" data-toggle="aria-haspopup" data-aria-expanded='false'> Your galleries </span>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% current_user.galeries.each do |gallery| %>
          <%= link_to gallery.name, gallery, class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
      <li class="nav-item dropdown">
        <span class="nav-link dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" data-toggle="aria-haspopup" data-aria-expanded='false'> Your exhibitions </span>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% current_user.exhibitions.each do |exhibition| %>
          <%= link_to exhibition.name, exhibition, class: "dropdown-item" %>
          <%end%>
        </div>
      </li>
      <li class="nav-item dropdown">
        <span class="nav-link dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" data-toggle="aria-haspopup" data-aria-expanded='false'> Menu </span>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if current_user.eql?(@showroom.exhibition.user) %>
          <%= link_to  "Add Piece", new_exhibition_art_piece_path(@showroom.exhibition) , :class => "dropdown-item" %>
          <%= link_to  "Edit Showroom", edit_showroom_path(@showroom) , :class => "dropdown-item" %>
          <%= link_to  "Delete Showroom", showroom_path(@showroom),:class => "dropdown-item", method: :delete, data: {confirm: "Are you sure?"} %>
          <%end%>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          <% else %>
          <%= link_to "Login", new_user_session_path, class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</div>
<div class="container">
  <div class="card-title text-center p-2 mb-1" style="margin-top:20px">
    <h1> <%= @showroom.name %></h1>
  </div>
</div>
<% if current_user.eql?(@showroom.exhibition.user) %>
<div class=container style="margin-top:20px">
  <div class="row">
    <div class="col-4">
      <div class="btn-group">
        <button type="button" class="btn btn-light dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Pick an image
        </button>
        <div class="dropdown-menu">
          <% @showroom.art_pieces.each do |art_piece| %>
          <%= art_piece.name %>
          <%end%>
        </div>
      </div>
    </div>
    <div class="col-4">
      <div class="btn-group">
        <button type="button" class="btn btn-light dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Pick an image
        </button>
        <div class="dropdown-menu">
          <% @showroom.art_pieces.each do |art_piece| %>
          <%= art_piece.name %>
          <%end%>
        </div>
      </div>
    </div>
    <div class="col-4">
      <div class="btn-group">
        <button type="button" class="btn btn-light dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Pick an image
        </button>
        <div class="dropdown-menu">
          <% @showroom.art_pieces.each do |art_piece| %>
          <%= art_piece.name %>
          <%end%>
        </div>
      </div>
    </div>
  </div>
</div>
<%end%>
<div class="container" style="margin-top:20px">
  <div class="card">
    <%= image_tag @showroom.background %>
  </div>
</div>